app = "familyos"
primary_region = "waw"

[build]
  dockerfile = "Dockerfile"

# ADMIN_USER_ID, API tokens and web auth are set with `fly secrets set`, not here.
[env]
  DATABASE_PATH = "/data/family.db"
  TZ = "Europe/Kyiv"

[[mounts]]
  source = "data"
  destination = "/data"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "off"   # otherwise the bot and the morning job go to sleep too
  auto_start_machines = true
  min_machines_running = 1

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
